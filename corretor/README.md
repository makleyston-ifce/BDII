# README

- Execute:
```
python3 -m venv venv
source venv/bin/activate
pip install pandas
```

- Use o comando para executar a correção. 
- Mude os caminhos exemplos para os seus caminhos
```
python corretor.py --expected ./gabarito --student ./respostas
```